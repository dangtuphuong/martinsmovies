name: martin-movie-app
app: martin-movie-app
stage: prod

component: "@sls-next/serverless-component@latest"

inputs:
  domain: null
  region: ap-southeast-2
  memory: 512
  timeout: 10
  bucketName: martin-movie-app-bucket
  env:
    TMDB_BEARER_TOKEN: ${env:TMDB_BEARER_TOKEN}
